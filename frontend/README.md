# AI Reel Studio Frontend

This frontend will use Next.js with Tailwind CSS to interact with the FastAPI backend.

## Local Development

- Start backend: `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`
- Start frontend: `npm run dev`
